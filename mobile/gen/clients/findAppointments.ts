/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  FindAppointmentsQueryResponse,
  FindAppointmentsQueryParams,
  FindAppointments401,
  FindAppointments403,
} from "../models/FindAppointments.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";

function getFindAppointmentsUrl() {
  const res = { method: "GET", url: `/api/appointments` as const };
  return res;
}

/**
 * @description Retrieves a paginated list of appointments with optional filtering and sorting
 * @summary Get appointments
 * {@link /api/appointments}
 */
export async function findAppointments(
  params?: FindAppointmentsQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    FindAppointmentsQueryResponse,
    ResponseErrorConfig<FindAppointments401 | FindAppointments403>,
    unknown
  >({
    method: "GET",
    url: getFindAppointmentsUrl().url.toString(),
    params,
    ...requestConfig,
  });
  return res.data;
}
