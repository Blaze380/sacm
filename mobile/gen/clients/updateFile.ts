/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  UpdateFileMutationRequest,
  UpdateFileMutationResponse,
  UpdateFilePathParams,
  UpdateFileQueryParams,
  UpdateFile400,
  UpdateFile404,
} from "../models/UpdateFile.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import { buildFormData } from "../.kubb/config.ts";

function getUpdateFileUrl(
  fileType: UpdateFilePathParams["fileType"],
  fileName: UpdateFilePathParams["fileName"],
) {
  const res = {
    method: "PATCH",
    url: `http://kami-no-notebook:8000/api/uploads/${fileType}/${fileName}` as const,
  };
  return res;
}

/**
 * @description Replace an existing file with a new one. Deletes the old file and uploads the new one.
 * @summary Update existing file
 * {@link /api/uploads/:fileType/:fileName}
 */
export async function updateFile(
  fileType: UpdateFilePathParams["fileType"],
  fileName: UpdateFilePathParams["fileName"],
  data: UpdateFileMutationRequest,
  params?: UpdateFileQueryParams,
  config: Partial<RequestConfig<UpdateFileMutationRequest>> & {
    client?: Client;
  } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const requestData = data;
  const formData = buildFormData(requestData);
  const res = await request<
    UpdateFileMutationResponse,
    ResponseErrorConfig<UpdateFile400 | UpdateFile404>,
    UpdateFileMutationRequest
  >({
    method: "PATCH",
    url: getUpdateFileUrl(fileType, fileName).url.toString(),
    params,
    data: formData as FormData,
    ...requestConfig,
  });
  return res.data;
}
