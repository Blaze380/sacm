export type ProfilePersonalFormFields = {
  firstName: string;
  lastName: string;
  phone: string;
  birthDate: Date;
};

export type ProfileAddressFormFields = {
  province: string;
  city: string;
  neighborhood: string;
};
